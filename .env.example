# Base URL for the API. Leave blank to use same-origin relative URLs
# (the Vite dev server proxies /api -> the backend).
VITE_API_BASE_URL=

# Dev proxy target. The backend runs on 8100 by default (8000 is often
# taken by other local services).
VITE_PROXY_TARGET=http://localhost:8100
